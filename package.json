{
  "dependencies": {
    "@fancyapps/ui": "^5.0.36",
    "lightgallery": "^2.8.3",
    "photoswipe": "^5.4.4",
    "vue-easy-lightbox": "^1.19.0"
  },
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend ci --no-progress --ignore-scripts && npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "deploy": "npm --prefix frontend run deploy"
  }
}
